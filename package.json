{
  "name": "kylejameswalker-site",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/build-apps.mjs && node scripts/minify-zine.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "test": "npm run test --workspaces --if-present",
    "build:zine": "node scripts/minify-zine.mjs",
    "check:portable": "node scripts/check-portable.mjs",
    "check:links": "node scripts/check-links.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.1",
    "@tailwindcss/vite": "^4.1.18",
    "astro": "^7.3.3",
    "tailwindcss": "^4.1.18",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.12"
  }
}
